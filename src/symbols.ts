export default class Symbols {
  static table: Symbol = Symbol('table');
  static hashKey: Symbol = Symbol('hashKey');
  static attribute: Symbol = Symbol('attribute');
  static version: Symbol = Symbol('version');
}
